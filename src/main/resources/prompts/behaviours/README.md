# Behaviours

This defines how the chatbot should behave, independent of personality.

For example:

- Should it ask the user a lot of questions? 
- How should it talk about what it knows about the user?